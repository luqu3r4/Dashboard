services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - dataprotection-keys:/keys
    restart: unless-stopped

volumes:
  dataprotection-keys:
